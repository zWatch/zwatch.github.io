const_cast<>()需要写类型

```c++
template<typename T>
T& const_cast_auto(const T& var)
{
	return const_cast<T&>(var);
}

template<typename T>
T* const_cast_auto(const T* var)
{
	return const_cast<T*>(var);
}
```

`