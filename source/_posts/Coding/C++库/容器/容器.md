```c++
array<T, Size>
vector<T>
deque<T> //双端队列，不保证连续
list<T> //double linked list
forward_list<T> //singly-linked list    


```

```

测试
inline bool end(cap, iter){ return iter == std::end(cap); }

value value_or(map, key, or_value);
value value_or_exception(map, key, std::source_location&=)

```

